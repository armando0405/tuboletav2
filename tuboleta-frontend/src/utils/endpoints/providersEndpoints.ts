// ProviderController: /api/providers/** (catálogo público de fuentes ACTIVE,
// REQ-FUE-001, para elegir proveedor(es) al crear una búsqueda). Distinto de
// /api/admin/providers (solo ADMIN, ver adminProvidersEndpoints.ts).
const CONTROLLER_PROVIDERS = '/providers'

const GET_PROVIDERS = CONTROLLER_PROVIDERS

export { GET_PROVIDERS }
